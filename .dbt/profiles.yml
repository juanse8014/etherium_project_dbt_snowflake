etherium_project:
  outputs:
    ci:
      account: "{{ env_var('DBT_SNOWFLAKE_IDENTIFIER') }}"
      database: DBT_CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: "{{ env_var('DBT_SNOWFLAKE_ROLE') }}"
      schema: DBT_SNAVARRETE
      threads: 1
      type: snowflake
      user: "{{ env_var('DBT_SNOWFLAKE_USER') }}"
      warehouse: COMPUTE_WH 
  target: ci
