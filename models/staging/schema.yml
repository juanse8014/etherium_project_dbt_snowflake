version: 2

models:
  - name: stg_transactions
    description: "Staging table for transactions data"
    columns:
      - name: hash
        description: "This is a very long multiline description about the unique identifier of each transaction in the Etherium Blockchain"
        data_tests:
          - not_null
          - unique
          # - relationships:
          #     arguments:
          #       to: ref('stg_token_transfers')
          #       field: transaction_hash
          #       severity: warn
          #       config:
          #         store_failures: true
          #         store_failures_as: table
          #         where: date >= current_date - 30
  - name: stg_token_transfers
    description: "Staging table for token transfers data"
    columns:
      - name: transaction_hash
        data_tests:
          - relationships:
              arguments:
                to: ref('stg_transactions')
                field: hash